# MiniShinobi Cloudflare Tunnel Configuration
# Route both minishinobi and all project subdomains to nginx (port 4000).

tunnel: <TUNNEL_ID>
credentials-file: /data/data/com.termux/files/home/.cloudflared/<TUNNEL_ID>.json

ingress:
  - hostname: minishinobi.<yourdomain.com>
    service: http://localhost:4000

  - hostname: '*.<yourdomain.com>'
    service: http://localhost:4000

  - service: http_status:404
